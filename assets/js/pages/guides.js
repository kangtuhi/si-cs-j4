document.addEventListener('DOMContentLoaded',()=>{let category='';const cats=[...new Set(guidesData.map(x=>x.category))];const filters=document.getElementById('guideFilters');filters.innerHTML=['Semua',...cats].map(c=>`<button class="filter-chip ${c==='Semua'?'active':''}" data-c="${c==='Semua'?'':c}">${c}</button>`).join('');function render(){const list=filterItems(guidesData,document.getElementById('guideSearch').value,category);document.getElementById('guidesContainer').innerHTML=list.map(g=>`<div class="col-md-6 col-lg-4"><div class="guide-card card"><div class="card-body"><div class="guide-icon"><i class="bi ${g.icon}"></i></div><span class="badge text-bg-primary mt-3">${g.category}</span><h5 class="mt-3">${g.title}</h5><p class="text-secondary">${g.description}</p><div class="guide-meta"><span>${g.level}</span><span>${g.duration}</span></div><button class="btn btn-sm btn-outline-primary mt-3" data-id="${g.id}">Lihat Panduan</button></div></div></div>`).join('');document.getElementById('emptyGuides').classList.toggle('d-none',!!list.length);document.querySelectorAll('[data-id]').forEach(b=>b.onclick=()=>openGuideModal(guidesData.find(g=>g.id==b.dataset.id)))}filters.onclick=e=>{if(e.target.matches('.filter-chip')){category=e.target.dataset.c;document.querySelectorAll('.filter-chip').forEach(b=>b.classList.remove('active'));e.target.classList.add('active');render()}};document.getElementById('guideSearch').oninput=render;render()});