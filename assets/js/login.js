document.addEventListener("DOMContentLoaded", () => {
  if (isAuthenticated()) { const redirect=new URLSearchParams(location.search).get("redirect"); location.replace(redirect||"dashboard.html"); return; }
  const form=document.getElementById("loginForm"),username=document.getElementById("loginUsername"),password=document.getElementById("loginPassword"),remember=document.getElementById("rememberLogin"),message=document.getElementById("loginMessage"),submit=document.getElementById("loginSubmit"),toggle=document.getElementById("togglePassword");
  document.getElementById("loginYear").textContent=new Date().getFullYear();
  toggle.addEventListener("click",()=>{const hidden=password.type==="password";password.type=hidden?"text":"password";toggle.innerHTML=`<i class="bi bi-eye${hidden?"-slash":""}"></i>`});
  document.getElementById("forgotDemo").addEventListener("click",()=>{message.className="login-message info show";message.innerHTML=`<i class="bi bi-info-circle"></i> Ini adalah akun demo portfolio. Gunakan kredensial yang tertera pada catatan di samping.`});
  document.getElementById("useDemoAccount")?.addEventListener("click",()=>{username.value="admin";password.value="sicsj4";username.focus();message.className="login-message success show";message.innerHTML=`<i class="bi bi-check-circle-fill"></i> Akun demo siap digunakan. Klik Masuk ke Sistem.`});
  form.addEventListener("submit",event=>{event.preventDefault();message.className="login-message";if(!username.value.trim()||!password.value){message.className="login-message error show";message.innerHTML=`<i class="bi bi-exclamation-circle"></i> Username dan password wajib diisi.`;return}submit.classList.add("loading");submit.disabled=true;setTimeout(()=>{const result=loginDemo(username.value,password.value,remember.checked);if(result.success){message.className="login-message success show";message.innerHTML=`<i class="bi bi-check-circle-fill"></i> Authentication berhasil. Membuka dashboard...`;submit.classList.add("success");setTimeout(()=>{const redirect=new URLSearchParams(location.search).get("redirect");location.replace(redirect||"dashboard.html")},650)}else{submit.classList.remove("loading");submit.disabled=false;message.className="login-message error show";message.innerHTML=`<i class="bi bi-shield-exclamation"></i> ${result.message}`;password.select()}},850)});
  if(new URLSearchParams(location.search).get("logout")){message.className="login-message success show";message.innerHTML=`<i class="bi bi-check-circle-fill"></i> Anda telah berhasil keluar dari sistem.`}
});
